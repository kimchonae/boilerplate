export const theme = {
    color: {
        light: {
            bodyColor: "#FFFFFF",
        },
        dark: {
            bodyColor: "#000000",
        },
    },
};

export type ThemeType = typeof theme;
